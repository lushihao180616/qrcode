<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lushihao.qrcode.dao.QRTempleMapper">
    <insert id="create" parameterType="QRCodeTemple">
        INSERT INTO
            qrcodetemple
            (code,money,ifOnly,ifShowLogo,ifSelfBg,arti,width,height,iconNum,x,y,angle)
        VALUES
            (#{code},#{money},#{ifOnly},#{ifShowLogo},#{ifSelfBg},#{arti},#{width},#{height},#{iconNum},#{x},#{y},#{angle})
    </insert>
    <update id="update" parameterType="QRCodeTemple">
        UPDATE
            qrcodetemple
        SET
            money=#{money},
            ifOnly=#{ifOnly},
            ifShowLogo=#{ifShowLogo},
            ifSelfBg=#{ifSelfBg},
            arti=#{arti},
            width=#{width},
            height=#{height},
            iconNum=#{iconNum},
            x=#{x},
            y=#{y},
            angle=#{angle}
        WHERE
            code=#{code}
    </update>
    <delete id="delete">
        DELETE FROM
            qrcodetemple
        WHERE
            code=#{code}
    </delete>
    <select id="filter" resultType="QRCodeTemple">
        SELECT
            *
        FROM
            qrcodetemple
        <if test="code != null">
            WHERE code
            LIKE '${code}%'
        </if>
    </select>
</mapper>